import { useEffect, useState } from "react";
import { getCookie } from "cookies-next";
import { createHash } from "crypto"

function useAdmin() {
	useEffect(() => {
		let password = getCookie("jsimian_admin");
		if(password.hash())

	});
	const [admin, setAdmin] = useState(false);
	return admin;
}

export default isAdmin;
